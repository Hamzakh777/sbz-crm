const getters = {
    isLoading(state) {
        return state.isLoading;
    },

    compensation(state) {
        return state.compensation;
    },

    totalExpectedProvision(state) {
        
    }
}

export default getters;